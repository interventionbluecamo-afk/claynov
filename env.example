# Claude AI API Configuration
# ⚠️ SECURITY: API key MUST be stored server-side only (in Vercel environment variables)
# Do NOT use VITE_ prefix - this would expose the key to the browser!

# Server-side environment variables (add these in Vercel dashboard):
# CLAUDE_API_KEY=sk-ant-xxxxx (your Anthropic API key)
# CLAUDE_API_VERSION=2024-02-15-preview (optional, defaults to this)
# CLAUDE_MODEL=claude-3-5-sonnet-20241022 (optional, defaults to this)

# The API key is used only in the serverless function at /api/optimize.js
# It is NEVER exposed to the client-side code

# Stripe Payment Configuration
# Option 1: Use Stripe Payment Links (Easiest - No backend needed)
# Get your Payment Link from: https://dashboard.stripe.com/payment-links
VITE_STRIPE_PAYMENT_LINK=https://buy.stripe.com/6oU5kv9kV4kSbUf79rcAo00

# Option 2: Use Stripe Checkout (Requires backend)
# Get your publishable key from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Supabase Configuration (for user accounts)
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

